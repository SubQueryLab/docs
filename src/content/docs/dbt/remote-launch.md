---
title: удаленный запуск
---


получаем токен

примеры запуска с токеном

в ответ айди таска

проверять статус таска

```js
fetch('https://subquery.ru/api/dbt/run?launchId=••••••', // Вместо точек вставляется launchId запуска, который приходит в ответе при создании запуска через API
  {
  headers: { 'Authorization': 'Bearer ••••••••••••••••••••••••••••••••' } // Вместо точек вставляется персональный токен
})
```
